<!DOCTYPE html>
<html lang="en">
<head>
  <title>Todo List APP ✨</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: linear-gradient(135deg, #667eea, #764ba2);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 50px 20px;
      color: #fff;
    }

    .container {
      background: rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      padding: 30px;
      max-width: 500px;
      width: 100%;
      backdrop-filter: blur(10px);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      animation: fadeIn 0.8s ease-in-out;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h1 {
      text-align: center;
      font-size: 22px;
      margin-bottom: 25px;
    }

    ul {
      list-style: none;
    }

    ul li {
      background: rgba(255, 255, 255, 0.15);
      padding: 15px 20px;
      border-radius: 12px;
      margin-bottom: 12px;
      color: #fff;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: background 0.3s ease;
    }

    ul li:hover {
      background: rgba(255, 255, 255, 0.25);
    }

    .btn-group {
      display: flex;
      gap: 12px;
    }

    .delete-btn,
    .edit-btn {
      text-decoration: none;
      font-size: 18px;
      transition: transform 0.2s ease;
    }

    .delete-btn {
      color: #ff6b6b;
    }

    .edit-btn {
      color: #ffd93d;
    }

    .delete-btn:hover,
    .edit-btn:hover {
      transform: scale(1.2);
    }

    form {
      margin-top: 25px;
    }

    label {
      font-weight: 600;
      margin-bottom: 8px;
      display: block;
      color: #eaeaea;
    }

    input[type="text"] {
      width: 100%;
      padding: 12px;
      border-radius: 10px;
      border: none;
      outline: none;
      font-size: 16px;
      margin-bottom: 16px;
    }

    input[type="submit"] {
      width: 100%;
      padding: 12px;
      border: none;
      background-color: #00d4ff;
      color: #fff;
      font-size: 16px;
      font-weight: bold;
      border-radius: 10px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    input[type="submit"]:hover {
      background-color: #00b8e6;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>📝 Hello Junoon Batch 8 — Plan Your Jenkins Learning!</h1>
    <ul>
      <% todolist.forEach(function(todo, index) { %>
        <li>
          <span><%- todo %></span>
          <div class="btn-group">
            <a href="/todo/<%= index %>" class="edit-btn" title="Edit">✎</a>
            <a href="/todo/delete/<%= index %>" class="delete-btn" title="Delete">✘</a>
          </div>
        </li>
      <% }); %>
    </ul>

    <form action="/todo/add/" method="post">
      <label for="newtodo">Add a new task ✨</label>
      <input type="text" name="newtodo" id="newtodo" placeholder="Type something amazing..." autofocus />
      <input type="submit" value="Add Task" />
    </form>
  </div>
</body>
</html>
